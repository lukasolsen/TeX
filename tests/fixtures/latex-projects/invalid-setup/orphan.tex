% This example is documentation only: \documentclass{article}
% !TeX root = /tmp/outside.tex
% !TeX root = ../simple-article/main.tex
% !TeX root = missing.tex

This fragment has no valid project root and cannot be compiled on its own.
